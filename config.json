{
  "server": {
    "host": "127.0.0.1",
    "port": 8090
  },
  "ww": {
    "watch": false,
    "root": "",
    "_note": "Isolation is triggered when a folder is ATTACHED to WOLFSPACE (POST /ww/attach), not by a fixed-root watcher — a folder can live anywhere. Set watch:true only if you want every new folder under 'root' isolated automatically. Leave 'root' empty to use the default; this file SHIPS IN THE INSTALLER, so a path here becomes every user's default."
  },
  "verbose": true,
  "eraser": {
    "apiKey": ""
  }
}
