# Third-party notices

WOLFSPACE is MIT licensed (see `LICENSE`). It also includes material from the
projects below, each under its own licence. This file records what was taken
and from where, so the obligation travels with the code rather than living in
someone's memory.

## Visual Studio Code — terminal colour palette

- Project: https://github.com/microsoft/vscode
- Licence: MIT
- Copyright (c) 2015 - present Microsoft Corporation
- Taken: the 16 ANSI colour values and `terminal.foreground`, from
  `src/vs/workbench/contrib/terminal/common/terminalColorRegistry.ts`
  at commit `db46a82c4f4a77c4853867dcdba5057229b5d099`
- Used in: `public/app/TemaTerminalVSCode.ts`

## Visual Studio Code — terminal source, vendored for reference

- Project: https://github.com/microsoft/vscode
- Licence: MIT (`vendor/vscode-terminal/LICENSE.txt`, copied with the source)
- Copyright (c) 2015 - present Microsoft Corporation
- Taken: `src/vs/workbench/contrib/terminal/` and `src/vs/platform/terminal/`
  in full, unmodified, at commit `db46a82c4f4a77c4853867dcdba5057229b5d099`
  — 179 files, 51,361 lines
- Kept in: `vendor/vscode-terminal/`

This copy is reference material, not code on the build path. Nothing imports
it, it sits outside every typecheck and lint scan, and `build.files` does not
list `vendor/`, so it does not reach the installer. See
`vendor/vscode-terminal/README.md` for why it cannot simply be compiled.

Note that the MIT licence covers the **source repository** above. The
Microsoft-branded Visual Studio Code product that ships as an installer is
distributed under a separate proprietary licence, and nothing here comes from
it.

## material-icon-theme — per-language file icons

- Licence: MIT
- Taken: a subset of the icon SVGs, for extensions that appear in the file tree
- Used in: `public/app/IkonBahasa.ts`, generated by
  `scripts/ikon-bahasa/build.cjs`

## xterm.js and node-pty

Declared as dependencies in `package.json` (both MIT). `public/vendor/xterm/`
holds published build output, loaded by `<script>` rather than bundled.

### xterm addons — vendored build output, MIT

These are **not** in `package.json`. Their published UMD builds are committed
under `public/vendor/xterm/` and loaded by `<script>`, the same way the xterm
core is:

| file                 | package                 | version        |
| -------------------- | ----------------------- | -------------- |
| `addon-fit.js`       | `xterm-addon-fit`       | (pre-existing) |
| `addon-webgl.js`     | `xterm-addon-webgl`     | (pre-existing) |
| `addon-search.js`    | `xterm-addon-search`    | 0.13.0         |
| `addon-web-links.js` | `xterm-addon-web-links` | 0.9.0          |

The `0.13.0` / `0.9.0` line is the one whose peer range is `xterm ^5.0.0`,
matching the xterm 5.3 in use. VS Code itself runs `@xterm/*` 6.x builds that
are not published to npm at those versions, so its exact set cannot be
installed even deliberately.
