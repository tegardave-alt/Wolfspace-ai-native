{
  "modes": {
    "plan": {
      "allowed_tools": [
        "list",
        "glob",
        "read",
        "grep",
        "disk_list",
        "disk_read",
        "disk_glob",
        "disk_grep",
        "web_search",
        "web_fetch",
        "skill_list",
        "todowrite",
        "question",
        "dspy",
        "task",
        "bash"
      ],
      "forbidden_tools": [
        "edit",
        "write",
        "terminal_open",
        "terminal_write",
        "terminal_read",
        "terminal_close",
        "skill_run",
        "skill_install",
        "sandbox_run"
      ],
      "refusal": "[PLAN MODE] Read-only — READ & ANALYZE ONLY. Tools: list/glob/read/grep/bash/disk_*/web_*/skill_list/task/todowrite/question/dspy.
WAJIB cari sampai tuntas sebelum menyimpulkan. Pola:
  1. bash untuk git/disk/fs langsung (git log, dir, type, test-path)
  2. grep keyword/frasa unik pendek
  3. Jika kosong → glob cari file pattern
  4. Jika masih kosong → disk_grep/disk_glob (luas, di luar project)
  5. web_search/web_fetch jika butuh konteks eksternal
  6. task untuk delegasi sub-eksplorasi paralel
Jangan simpulkan 'tidak ada' sebelum mencoba minimal 3 metode berbeda.
Jika ragu, verifikasi pakai tool lain. BASH HANYA UNTUK READ-ONLY (git log, dir, cat, type, test-path). DILARANG edit/write/echo > via bash."
    },
    "build": {
      "allowed_tools": ["all"],
      "forbidden_tools": [],
      "refusal": "[BUILD MODE] Semua tool aktif — siap coding, edit, dan execute."
    }
  }
}
